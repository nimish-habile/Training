--1. Using FOR UPDATE OF:
--Write a PL/SQL block to increase the salary of employees in department 60,80 and 90 by 10% using FOR UPDATE OF. Use the parameterized cursor.
--DECLARE
--CURSOR EMP_CURSOR (P_DEPT_ID EMPLOYEES.DEPARTMENT_ID%TYPE)
--IS
--SELECT EMPLOYEE_ID, SALARY FROM EMPLOYEES
--WHERE DEPARTMENT_ID = P_DEPT_ID FOR UPDATE OF SALARY;
--  V_DEPT_ID EMPLOYEES.DEPARTMENT_ID%TYPE;
--  V_EMP_ID EMPLOYEES.EMPLOYEE_ID%TYPE;
--  V_SAL EMPLOYEES.SALARY%TYPE;
--  CURSOR DEPT_CURSOR
--  IS
--    SELECT 60 DEPT_ID FROM DUAL UNION SELECT 80 FROM DUAL UNION SELECT 90 FROM DUAL;
--BEGIN
--  FOR V_DEPT_ID IN DEPT_CURSOR
--  LOOP
--    DBMS_OUTPUT.PUT_LINE(TO_CHAR(V_DEPT_ID.DEPT_ID));
--    OPEN EMP_CURSOR(V_DEPT_ID.DEPT_ID);
--    LOOP
--      FETCH EMP_CURSOR INTO V_EMP_ID, V_SAL;
--      EXIT
--    WHEN EMP_CURSOR%NOTFOUND;
--      UPDATE EMPLOYEES SET SALARY = SALARY * 1.1 WHERE CURRENT OF EMP_CURSOR;
--    END LOOP;
--    CLOSE EMP_CURSOR;
--  END LOOP;
--  COMMIT;
--END;

--2. Using WHERE CURRENT OF:
--ASSUME YOU HAVE A CURSOR "emp_cursor" THAT SELECTS EMPLOYEE RECORDS FROM THE "Employees" TABLE. 
--Write a PL/SQL block to update the salary of the current row fetched by the cursor using WHERE CURRENT OF.

--DECLARE
--  CURSOR SAL_CUR
--  IS
--    SELECT DEPARTMENT_ID,SALARY FROM EMPLOYEES FOR UPDATE OF salary nowait ;
--BEGIN
--  FOR EMP_RECORD IN SAL_CUR
--  LOOP
--    IF emp_record.salary<6000 THEN
--      UPDATE EMPLOYEES SET SALARY=EMP_RECORD.SALARY*1.1 WHERE CURRENT OF SAL_CUR;
--    END IF;
--  END LOOP;
--END;
